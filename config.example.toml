[api]
# OpenAI API key (or set OPENAI_API_KEY environment variable)
openai_api_key = "sk-..."

# Model to use for vision
model = "gpt-4o"

[agent]
# Maximum actions per task before stopping
max_actions = 50

# Delay in seconds between actions
action_delay = 1.0

# Screenshot resolution (width). Height scales proportionally.
screenshot_width = 1024

[vm]
# Keywords to match VM window titles (comma-separated)
window_keywords = ["vmconnect", "vmware", "virtualbox", "qemu", "hyper-v"]
